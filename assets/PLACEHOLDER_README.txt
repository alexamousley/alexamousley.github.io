PLACEHOLDER — REPLACE THIS FILE

Save your headshot as `headshot.jpg` in this folder, replacing this file.

Specs:
- Square crop (e.g. 800 x 800 pixels minimum)
- Head and shoulders, eyes roughly a third from the top
- Plain or softly-blurred background
- Natural lighting if possible

Also save your CV PDF in this folder as `Mousley_CV.pdf`.

You can also drop a `favicon.png` (32x32 or 64x64) in this folder. Most academics use a simple letter or icon, but it's optional — the site works fine without it (just remove the favicon line in _quarto.yml if you skip it).
